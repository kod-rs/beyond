---
$schema: 'http://json-schema.org/draft-07/schema#'
id: 'flexopt://backend_requests/buildings_info_request.yaml#'
title: Buildings info request
description: Buildings info configuration schema
type: object
required:
  - type
  - building_ids
  - signature
properties:
  type:
    const: building_info_request
  building_ids:
    type: array
    items:
      type: string
  signature:
    type: string
    description: Hash function signed with the sender private key calculated
      over the rest of the message (excluding signature)
...
