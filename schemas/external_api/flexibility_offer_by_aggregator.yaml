---
$schema: 'http://json-schema.org/draft-07/schema#'
id: 'flexopt://external_api/flexibility_offer_by_aggregator.yaml#'
title: Flexibility offer by aggregator id request
description: Flexibility offer by aggregator id request configuration schema
type: object
additionalProperties: false
required:
  - type
  - start_time
  - end_time
  - user_id
  - signature
properties:
  type:
    const: flexibility_offer_by_aggregator
  start_time:
    type: string
    format: "date-time"
  end_time:
    type: string
    format: "date-time"
  user_id:
    type: string
  signature:
    type: string
    description: Hash function signed with the sender private key calculated
      over the rest of the message (excluding signature)
...
