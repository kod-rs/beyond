import { Outlet } from 'react-router-dom';

/*import Directory from '../../components/directory/directory.component';*/

const Home = () => {
  return (
    <div>
          {
              //TODO MAKE 1st page
          }
        <Outlet />
    </div>
  );
};

export default Home;
